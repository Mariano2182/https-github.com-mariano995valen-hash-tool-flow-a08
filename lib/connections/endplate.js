// lib/connections/endplate.js
export function buildEndPlates(_building, _opts = {}) {
  // Placeholder: acá entran uniones tipo:
  // - END PLATE viga-columna
  // - SPLICE (empalme) en columnas/vigas
  // - RIDGE PLATE / APEX CONNECTION
  return [];
}
